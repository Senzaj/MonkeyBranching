using UnityEngine;

namespace Project.EmergingObjectsAndPlayer.Scripts
{
    public class Nowhere : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
